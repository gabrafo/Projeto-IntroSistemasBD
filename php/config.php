<?php
    // Configurações de conexão com o banco de dados
    
    // Endereço do servidor MySQL (normalmente 'localhost' em ambientes locais)
    $host = "localhost";
    
    // Usuário do banco de dados (padrão XAMPP/WAMP é 'root')
    $login = "root";
    
    // Senha do banco de dados (vazia por padrão em ambientes de desenvolvimento)
    $senha = "";
    
    // Nome do banco de dados que será utilizado
    $bd = "projetoISBD";
    
    // Estas variáveis são usadas posteriormente para estabelecer a conexão MySQLi
    // Exemplo de uso: new mysqli($host, $login, $senha, $bd);
?>
